# Railway configuration for API service (root service)
# This runs the API from apps/api

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node apps/api/src/index.js"
healthcheckPath = "/api/health"
healthcheckPort = 4000
restartPolicyType = "ON_FAILURE"
